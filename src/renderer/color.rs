/// [`Color`] contains common colors used throughout the game.
#[derive(Clone, Copy, PartialEq)]
pub enum Color {
    Black,
    White,
    Red,
    Green,
    Blue,
    Yellow,
    Cyan,
    Magenta,
    Brown,
    DarkGrey,
}